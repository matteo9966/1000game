{
  "name": "1000goals",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint",
    "nodemon-run": "nodemon build/src",
    "watch": "tsc-watch && npm run nodemon-run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "nodemon": "^2.0.22",
    "tsc-watch": "^6.0.4",
    "typescript": "~4.7.0"
  },
  "dependencies": {
    "node-json-db": "^2.2.0"
  }
}
