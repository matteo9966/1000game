// import {expect} from 'chai';
// import * as dotenv from 'dotenv';
// import { join } from 'path';
// describe('environment variables', () => {
//     beforeEach(()=>{
//         dotenv.config({path: './testenv/.env',override:true});
//     });
//   it('should set the environment variables from the test.env file when using mocha', () => {
//     expect(process.env.ENV).to.exist;
//     expect(process.env.ENV).to.equal('test');
//     expect(process.env.PORT).to.exist;
//     expect(process.env.PORT).to.equal('4000');
//     expect(process.env.PRIVATEKEY).to.exist;
//     expect(process.env.PUBLICKEY).to.exist;
//   });
// });
